{/* <section id="processes" className="py-40 bg-white m-0">
    <Container className="width-780 text-center mb-60">
        <header className="brd-primary heading--bottom text-uppercase mb-4">
            <h4 className="h6">Work process</h4>
            <h2 className="h1">{Data.processes.title}</h2>
        </header>
        <p>{Data.processes.description}</p>
    </Container>
    <Container>
        <Row>
            {Data.processes.steps.map((s, i) =>
                <Col key={i} lg={6} className="px-3 mb-4">
                    <div className="brd-primary--hover brd-dark brd-3 brd-left py-3 px-2 bg-light px-25">
                        <h3 className="h5 text-uppercase">{s.step}</h3>
                        <p>{s.description}</p>
                    </div>
                </Col>
            )}
        </Row>
    </Container>
</section> */}

{/* <section id="howWeWork" className="m-0">
    <Row className="no-gutters">
        <Col lg={5} className="bg-img-hero bg-sample"></Col>
        <Col lg={7} className="p-5">
            <header className="text-uppercase heading--bottom brd-primary mb-4">
                <h4 className="h6">How we work</h4>
                <h2 className="h1">{Data.howWeWork.title}</h2>
            </header>
            <p className="mb-65">{Data.howWeWork.description}</p>
            <Row className="align-items-stretch">
                {Data.howWeWork.items.map((s, i) =>
                    <Col key={i} sm={6} className="mb-4">
                        <div className="bg-light brd-primary--hover brd-dark brd-3 brd-left py-3 px-2 px-25">
                            <h4 className="h6 text-uppercase">{s.name}</h4>
                            <p>{s.description}</p>
                        </div>
                    </Col>
                )}
            </Row>
        </Col>
    </Row>
</section> */}